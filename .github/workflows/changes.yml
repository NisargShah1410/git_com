name: Commit date to master
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
jobs:
  commit_history:
    runs-on: ubuntu-latest
    steps:
      # Checkout the branch
      - name: checkout
        uses: actions/checkout@v2

      - name: setup git config
        uses: nisargshah1410/git_com@v1.3
        with:
          github_handle: NisargShah1410
          github_mail: nisargshah01410@gmail.com
          repository: git_com
          
