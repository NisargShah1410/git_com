name: Git commit history
author: NisargShah1410
description: 'Returns git commit history'

inputs:
  github_handle:
    description: 'GitHub Handle name'
    required: true
  repository:
    description: 'Repository'
    required: true

runs:
  using: 'node12'
  main: 'index.js'